# Production Requirements for Vercel
# Only lightweight dependencies are included to meet Vercel's 250MB limit

# PDF processing (if still used)
PyPDF2==3.0.1
pdfplumber==0.10.3

# Utilities
python-dotenv==1.0.0
deep-translator==1.11.4
requests==2.31.0

# Data processing
numpy==1.24.3
pandas==2.0.3

# Web server
flask==3.0.0
flask-cors==4.0.0
python-docx==1.1.0
